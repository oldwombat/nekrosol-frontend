/**
 * Shared test credentials — the backend auto-creates a player on first login
 * if the email doesn't exist, so no seed step is needed.
 */
export const testPlayer = {
  email: 'playwright-test@nekrosol.local',
  password: 'playwright-test-password-123',
}
